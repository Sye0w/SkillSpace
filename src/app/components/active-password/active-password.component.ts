import { Component } from '@angular/core';
import { RouterModule } from '@angular/router';

@Component({
  selector: 'app-active-password',
  standalone: true,
  imports: [RouterModule],
  templateUrl: './active-password.component.html',
  styleUrl: './active-password.component.scss'
})
export class ActivePasswordComponent {

}
